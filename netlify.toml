[build]
  publish = "."
  command = "node scripts/generate-env.js"

[build.environment]
  NODE_VERSION = "18"
  # Skip secret detection - these API keys are intentionally client-side
  NETLIFY_SKIP_SECRETS_CHECK = "true"
